# Pass B: Audience Impact / Expressive Readability

VERDICT: PASS
Confidence: High
Evidence: decks/slo/gate-preview/slide-01.png, decks/slo/gate-preview/slide-02.png, decks/slo/gate-preview/slide-03.png, decks/slo/gate-preview/slide-04.png, decks/slo/gate-preview/slide-05.png, decks/slo/gate-preview/slide-06.png
Slide fingerprints: slide-01.html: 4f89c8c211fff52895dde20bec8933e0e284df486103372cbf0a2206d0724bc9, slide-02.html: 408f1a9a49c2533d2b2309ef51e893e95b39d890294c0debfd0e8d086e092c20, slide-03.html: add6ce4969ab759ff0e72119b7c800356d4764e1068b98c621ced52465e2a081, slide-04.html: 1382d3689f928078ad92188b813b2f6b28d466aeb4112f2fa5ba7580fc5f0ac5, slide-05.html: 5ce76b2a4dd7e809fb6529a2751dc310a2220d10f42612266f0b7f33bfbd441d, slide-06.html: 73d01d1c6b7ce7a19b316f2afb523986df1973a60b12c5a3bb7077a50d790edf
Unresolved Critical: 0
Blocking findings: None

Method: all six PNGs from this render were opened individually as full-size images, not skimmed — six sheets is few enough to look at each one properly — and the contact sheet at `decks/slo/contact-sheets/sheet-01.png` was then opened to check the furniture across sheets side by side. The previous render was opened the same way; that is how the six defects listed below were found, **every one of which `validate` reported as passing (6/6, 0 errors)**. Element geometry was additionally read out of headless Chromium with `getBoundingClientRect`, measuring each line's advance width against its parent's real content box, to confirm that lines which must not wrap in fact do not.

## Checks
- [x] Composition & hierarchy: PASS — One job per sheet and one anchor per sheet. slide-01.png: the anchor is the three-line 36pt statement "What you promise, / and what you refuse / to promise." set against a mono contents index across a full-height hairline — a real visual anchor, not a title in a text box, and the index also tells the room the shape of the talk. slide-02.png: two panels, and the anchor is the 3pt black bar over the right one, which is the whole claim (the same number, used rather than read). slide-03.png: the anchor is the four-node process with the emphasis bar on `04 Stop the work` — the argument that the loop terminates in a stop is *shown*, not asserted. slide-04.png: a bounded ledger where the only marked column is the one the title says is the point. slide-05.png: three identical cards under one intro line, closed by a band. slide-06.png: three ruled decision rows closed by the deck's only 3px rule, which reads as a full stop. Both the cover and the closing sheet carry a genuine anchor.
- [x] Typography & legibility: PASS — One scale across the deck: display 36pt, title 23pt, decision heading 16pt, card/node header 15pt, statement band 15–16pt, body 14pt, mono label 11pt, mono caption 10pt. `grep` over every `font-size` in the deck returns a minimum of **10pt**, used only for the two footer captions; no body copy is below 14pt and nothing is below the absolute floor. Contrast: `#000000` on `#FFFFFF` is 21:1, and `#666666` on `#FFFFFF` is about 5.7:1 (about 5.3:1 on the `#F2F2F2` head row on 04) — above the 4.5:1 body threshold, and `#666666` is only ever used for mono labels and captions, never for prose. Checked on the renders at full size: the 10pt mono footer captions on slide-03.png and slide-06.png are legible, and the 11pt axis keys on slide-04.png read cleanly against the black cell rules. No text is invisible against its surface — there is no coloured panel anywhere for it to disappear into.
- [x] Korean/CJK word-break integrity: PASS (not applicable) — this is an English deck; there is no Hangul or CJK and Pretendard was deleted after scaffolding. English wrapping was inspected in its place, against measured advance widths rather than by eye, and the equivalent faults were found and fixed: two right-panel items on slide 02 wrapped to a second line while their left-panel counterparts did not, and the slide-06 closing statement overran its measure by 8.3pt and stranded "anything." on a line of its own. In the current render no title wraps, no line ends in an orphan word, the six panel items on slide-02.png are one line each, and every line that must be one line has at least 11.5pt of measured clearance.
- [x] Review Litmus: PASS — Three to five seconds per sheet. slide-03.png reads as `Name the target → Spend the budget → Hit the floor → Stop the work` with the last box heavier than the others, before any sentence is read. slide-04.png reads as a two-column ledger with the right column marked, so "what we refuse" registers as the load-bearing half without the title being needed. slide-06.png reads as three numbered decisions under a hard bottom rule. Stripping decoration is not available: there is no decoration — the visual vocabulary is type, hairlines and border weight, exactly as the spec requires. On removable lines: five were in fact removed or shortened during the render pass (three panel items on 02, one ledger cell on 04, one card header on 05, the closing statement on 06), and every one of those sentences got tighter for it. One further line was rejected before it was ever written: the slide-03 band's original two-sentence form needed 623.6pt of a 640pt measure and was cut to its first sentence, with the second idea folded into the right-hand column.

## Findings
| Slide | Finding | Severity | Fix | Status |
|-------|---------|----------|-----|--------|
| slide-02 | Two right-panel items wrapped to a second line while all three left-panel items were one line, so the panels' item hairlines and first lines sat at different y — the two columns visibly failed to align. `validate` passed it: each item is inside its own box, so it is neither an overflow nor a sibling overlap | Major | Fixed before this report: "Crossing it changes who decides what ships." → "Crossing it changes what ships next.", "It happens without anyone calling a meeting." → "No meeting is needed for it to fire.", and the left panel's tightest item shortened too. All six now have 11.7–58.9pt of measured clearance. Verified in the current slide-02.png | resolved |
| slide-06 | The closing statement overran its 639.8pt measure by 8.3pt, wrapped, and left "anything." alone on a second line that pushed into the footer zone | Major | Fixed: rewritten to "Until these three are answered, we have a metric — and metrics stop nothing.", now 586.3pt with 53.5pt of clearance and one line | resolved |
| slide-06 | The first decision row's `border-top` sat 44px below the header rule with nothing between them, reading as a doubled rule and an accidental empty band | Major | Fixed: the rule moved to `.drow + .drow` so it only ever separates rows | resolved |
| slide-06 | The mono `01`/`02`/`03` index was vertically centred against a two-element text block, so each number floated between its heading and its sentence instead of sitting with the heading | Minor | Fixed: row `align-items` changed from `center` to `flex-start`; the numerals now share a top edge with the headings they index | resolved |
| slide-04 | The `AXIS` and `OUT, BY NAME` head labels rendered at full ink while `IN THE PROMISE` rendered muted, so the head row read as if two of three columns were emphasised and the 3pt bar's meaning was diluted | Minor | Fixed: the two colour overrides were deleted; all mono labels are `#666666` and all prose is `#000000`, leaving the 3pt bar as the only emphasis signal | resolved |
| slide-04, slide-05 | "A maintenance window we declared" had 1.5pt of clearance in its cell and "An exception every time" had 0.9pt in its card — both technically one line, both visually touching the cell wall | Minor | Fixed: cell copy → "A declared maintenance window" (24.7pt clearance); card gap 16 → 12pt, card padding 14 → 12pt, header → "An exception each time" (11.5pt clearance) | resolved |
| slide-01 | The rule above the presenter placeholder stops 24pt short of the vertical column divider | Note | Accepted — it is the left column's own rule and the 24pt is the grid gutter, so it is consistent rather than short. Carried to design-debt.md | tracked |
| slide-03 | The two column bodies run to four lines each and leave about 24pt of unused height above the closing band | Note | Accepted — the columns carry a top hairline and the band carries its own, so the gap reads as the block's internal padding, not as a hole. Filling it would have meant inventing a third claim. Carried to design-debt.md | tracked |
| all | 프레임 하단 여백을 올려 최하단 텍스트가 아래 가장자리에서 38pt 위에 오게 했다. 파워포인트 text 엔진이 0.5in(36pt) 안전 여백을 요구한다 | Note | 편집 가능한 PPTX를 위한 변경. 넘침 0(실측), 컨택트 시트 재확인 결과 레이아웃 변화 없음 | fixed |
| slide-01/02/05 | 리스트 구분선과 카드 하단선을 `li`·`p`에서 래퍼 `<div>`로 옮겼다. text 엔진은 텍스트 요소의 border를 거부한다 | Note | `.ln`/`.cap` 래퍼 신설, 시각 결과 동일 | fixed |
| all | `<header>`·`<footer>` 를 `<div>`로, `<p>` 밖 `<span>`을 `<p>`로 바꿨다. 파워포인트 text 엔진은 두 경우 모두 글자를 **경고 없이 버린다** | Major | 실제 PPTX를 열어 HTML과 텍스트를 대조해 발견. 렌더 픽셀 차이 0 | fixed |
