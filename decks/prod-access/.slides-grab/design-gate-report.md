# slides-grab Design Gate Report

Verdict: proceed
Generated: 2026-08-27T16:15:22.233Z
Slide mode: presentation
Resolution: 2160p

## Pass A: System Contract / Constraint Integrity

# Pass A: System Contract / Constraint Integrity

VERDICT: PASS
Confidence: High
Evidence: decks/prod-access/gate-preview/slide-01.png, decks/prod-access/gate-preview/slide-02.png, decks/prod-access/gate-preview/slide-03.png, decks/prod-access/gate-preview/slide-04.png, decks/prod-access/gate-preview/slide-05.png, decks/prod-access/gate-preview/slide-06.png
Slide fingerprints: slide-01.html: 548e554a0ad33f1235544fc976ef22775154e2873db269f934ec990cf111274e, slide-02.html: ee19e577cba88829fb40960db51e5f5d1eb51745d590688d234acb035a06986d, slide-03.html: f532644eec63e5a519e9bd0b7cdb8b5cd2bb782ce953660e2a9e0fefb0b47909, slide-04.html: 45a22a868067477c2d8a2b062c9e665a754940674fa13ad8bce15899af186514, slide-05.html: 8cacb56730df5196c4f03be94b20796c1103862b4b155b7be962a3aee2797873, slide-06.html: b7e3771b82cbe4147dcbfefc59f5f48437d7ed465aab986a27e8e6b8848ec66b
Unresolved Critical: 0
Blocking findings: None

Method: the six slide sources were read line by line against the `show-design ppt-goldman-ir-deck` output. Every hex literal in the deck was extracted (`grep -oh '#[0-9A-Fa-f]\{6\}' slide-0*.html | sort -u`) and matched against the spec token list; every `font-size` was extracted the same way and sorted. Contrast ratios for all five inks against both surfaces were computed numerically (WCAG relative luminance) **before the first render**, not judged by eye — this is a dark deck and the brief is right that a muted grey can look fine and land near 2.7:1 on a dark ground. All six rendered PNGs above were then opened as full-size images.

## Checks
- [x] System consistency: PASS — One furniture system on all six sheets: an 11pt uppercase rail (context left, section right), a 24pt Source Serif 4 heading, a 0.5pt gold hairline divider, `main`, and a two-part footline (source caption left, disclaimer + pagination right), inside identical 40pt/32pt margins on a 640pt measure. Header and footer are **siblings** of `main` with `main{flex:1;min-height:0}`, so the gold divider and the footline sit at the same y on every content sheet — verified by flipping between slide-02.png, slide-04.png and slide-05.png, where the divider and both footline baselines do not move by a pixel. Sheets 01 and 06 drop the heading + divider (they are cover and closing) but keep the same rail, margins and footline y. Exactly **two backgrounds** (`#0A1A33` canvas, `#1F3A5F` panels), exactly **two typefaces** (Source Serif 4 for display, headings and claims; Inter for body, labels and footline), **one accent** (`#C8A24B`). Two content modules, each reused rather than reinvented: the ruled ledger row (02, and again for the prompts on 06) and the gold-hairline navy panel (03 as a triptych, 04 as a 2×2, 05 as a triptych with chevrons). No sheet invents a third.
- [x] Color discipline: PASS — The hex audit returns exactly six values: `#0A1A33`, `#1F3A5F`, `#3A5A85`, `#9AA6BC`, `#C8A24B`, `#E8E5DE`. Every one is a `ppt-goldman-ir-deck` spec token, verbatim. **No harmonic extension was needed and none was invented**, so there is no untracked colour to be Critical about. The computed ratios governed use rather than decorated it: `#3A5A85` is 2.47:1 on the canvas and is therefore confined to 1px row rules and never carries text; `#9AA6BC` is 7.08:1 on the canvas and 4.68:1 on a panel, so panel prose uses `#E8E5DE` (9.13:1) and `#9AA6BC` only appears on panels at 14pt where it clears 4.5; `#C8A24B` clears 4.5 on both grounds (7.22 / 4.77) and so is legitimate for the 11–12pt uppercase labels. Gold is never a fill — the Avoid list's first line — only 0.5pt hairlines, uppercase labels, step numbers, and one emphasised word on 01 and 06. `#1F3A5F` is only 1.51:1 against the canvas, which is exactly why every panel carries the spec's gold hairline border: without it the panel would not be visible as a panel.
- [x] AI slop tropes: PASS — `grep -ci 'gradient|box-shadow|border-radius|http'` returns **0 on all six files**. No gradient of any kind, including the radial-gradient pattern-tile trick; no shadow; no rounded corner (radius 0 holds literally — every panel, rule and divider is a hard rectangle or a hairline). No rounded card with a left stripe used as a default container. No SVG illustration: the only SVG in the deck is the two chevrons in slide-05's gutters, which are the spec's own `diagram.language` "restrained chevron arrowheads". No emoji, no icon, no clipart, no 3×2 icon grid. The font stack is not generic — Source Serif 4 and Inter are the two faces the spec names by name, embedded locally from `@fontsource/*` (136KB), which the framework explicitly exempts from the generic-stack prohibition. `http` appears nowhere in any saved slide.
- [x] Content discipline: PASS — **There is no figure anywhere in this deck.** No chart, no canvas, no percentage, no duration, no benchmark, no stat strip, no count. The only digits on screen are sheet numbers (01/06), ledger ordinals (01, 02, 03) and one clock time used rhetorically ("03:00 is when asking costs most" — a time of night, not a measurement). Nothing is dressed to look like data. Per the brief, the decision is recorded in `slide-outline.md` under "no figures, and why", and the fact is carried **in the style's mandatory source-caption slot** on every sheet rather than a citation: `SOURCE — none. No figures in this deck; the argument is mechanical.` That is the honest thing to put in a provenance slot when there is no provenance, and it satisfies the style's "출처 각주" requirement without fabricating one. The style's other mandatory furniture — the bottom-right disclaimer + pagination footline — is present on all six sheets, checked in every PNG.

## Findings
| Slide | Finding | Severity | Fix | Status |
|-------|---------|----------|-----|--------|
| all | The footline disclaimer is 10pt, where the spec specifies `disclaimer: Inter 8pt` | Major | **Deliberate deviation.** 8pt is below the framework's absolute 10pt floor and would be Critical here. The IR identity — a disclaimer + pagination pinned bottom-right on every sheet — is fully preserved; only the size moved. Recorded in slide-outline.md decision 2 and design-debt.md #1 | tracked |
| all | Point sizes are scaled up from the spec rather than copied (heading 26 → 24pt, body 17 → 15/14pt) | Note | Intentional — the spec targets a 13.33in canvas, this is 10in. Scaling literally gives body 12.75pt and disclaimer 6pt, both under the framework floors, so the scale is anchored at the floors instead. Recorded in slide-outline.md decision 1 | tracked |
| all | The spec's diagram vocabulary asks for a monospace face for step numbers and date labels; no monospace is used | Minor | **Deliberate deviation.** The style's own Typography block declares only two faces, and Pass A caps typefaces at two. Step numbers are Inter 600 12pt with `font-variant-numeric: tabular-nums` and +0.06em tracking in gold, which keeps the tabular wide-tracked reading without a third face. Recorded in slide-outline.md decision 4 and design-debt.md #2 | tracked |
| all | Body leading is 1.45 where the spec specifies 1.4, and serif claims are 1.4 | Note | Intentional — the framework floors body leading at 1.4 and this deck already had two `text-clipped` errors at 1.3 on 46pt and 34pt display type. `line-height: 1` appears nowhere (grep-verified) | tracked |
| slide-02 | The last ledger row's 1px navy rule is set to `transparent` rather than removed | Note | Deliberate — removing the border would change that row's box height and break the "emphasise one and only that one shifts" rule. Colouring it away keeps every row's box identical | tracked |
| slide-04 | The 2×2 record grid is a different arrangement from the 03/05 triptych | Note | Same module (gold-hairline navy panel), different arrangement, because four fields do not fit a triptych. Not a third pattern | tracked |
| all | `<header>`·`<footer>` 를 `<div>`로, `<p>` 밖 `<span>`을 `<p>`로 바꿨다. 파워포인트 text 엔진은 두 경우 모두 글자를 **경고 없이 버린다** | Major | 실제 PPTX를 열어 HTML과 텍스트를 대조해 발견. 렌더 픽셀 차이 0 | fixed |

## Pass B: Audience Impact / Expressive Readability

# Pass B: Audience Impact / Expressive Readability

VERDICT: PASS
Confidence: High
Evidence: decks/prod-access/gate-preview/slide-01.png, decks/prod-access/gate-preview/slide-02.png, decks/prod-access/gate-preview/slide-03.png, decks/prod-access/gate-preview/slide-04.png, decks/prod-access/gate-preview/slide-05.png, decks/prod-access/gate-preview/slide-06.png
Slide fingerprints: slide-01.html: 548e554a0ad33f1235544fc976ef22775154e2873db269f934ec990cf111274e, slide-02.html: ee19e577cba88829fb40960db51e5f5d1eb51745d590688d234acb035a06986d, slide-03.html: f532644eec63e5a519e9bd0b7cdb8b5cd2bb782ce953660e2a9e0fefb0b47909, slide-04.html: 45a22a868067477c2d8a2b062c9e665a754940674fa13ad8bce15899af186514, slide-05.html: 8cacb56730df5196c4f03be94b20796c1103862b4b155b7be962a3aee2797873, slide-06.html: b7e3771b82cbe4147dcbfefc59f5f48437d7ed465aab986a27e8e6b8848ec66b
Unresolved Critical: 0
Blocking findings: None

Method: **all six PNGs from this render were opened individually as full-size images**, not skimmed — six sheets is few enough to look at each one properly. The contact sheet at `contact-sheets/sheet-01.png` was opened as well, but only afterwards and only to check cross-sheet consistency (that the divider, the footline and the outer margin land at the same y on every sheet), which is the one thing a contact sheet is better at. The two earlier render passes were reviewed the same way; that is how the eight defects listed below were found, and `validate` reported **6/6 passing while all eight were on screen**. Element geometry was additionally read out of headless Chromium (`_measure-prodaccess.mjs`, since deleted) to confirm numerically that no container's `scrollHeight` exceeds its `clientHeight` on any sheet, rather than trusting that a collision merely looks resolved.

## Checks
- [x] Composition & hierarchy: PASS — One job per sheet, one anchor per sheet. slide-01.png: the anchor is two full-measure lines of 46pt Source Serif 4, the second in the deck's only gold — a real typographic anchor, not a title in a box — with the answer already visible in the hairlined panel beside the premise. slide-02.png: a bounded financial table, gold rule above and below, closing on a gold-ruled total row that states the mechanism in one line. slide-03.png: three equal panels under one question, closed by a gold-ruled note that turns the sheet ("which is why standing access wins"). slide-04.png: the schema sheet — four panels, four field names in gold, the requirement in serif and the failure mode underneath; this is the `iac-drift` one-liner given a whole sheet. slide-05.png: the same panel module in sequence with gold chevrons, so the three conditions read as a chain that has to close rather than a list. slide-06.png: the closing anchor is "What we / need to / decide" at 34pt with `decide` in gold, balanced against a gold-ruled ledger of three questions. **Both the cover and the closing sheet carry a genuine visual anchor.** Checked specifically on the contact sheet: the gold divider is at an identical y on 02–05 and the footline baseline is identical on all six.
- [x] Typography & legibility: PASS — One scale across the deck, verified by extracting every `font-size`: 46 / 34 / 24 / 16 / 15 / 14 / 12 / 11 / 10pt and nothing else. **Minimum is 10pt** (the footline, exactly at the floor and nowhere below it) and no body copy is below 14pt. `line-height: 1` appears nowhere; body leading is 1.45, serif claims 1.4, display 1.35. Two `text-clipped` errors at 1.3 on the 46pt cover and 34pt closing display were caught by `validate` and fixed by raising leading, not by shrinking type. Contrast was computed, not eyeballed: text 13.81:1 on the canvas and 9.13:1 on a panel; gold 7.22 / 4.77; muted 7.08 / 4.68. `#3A5A85` measured 2.47:1 on the canvas and was therefore removed from text duty entirely — it is only 1px row rules. **The presenter line was checked by name**, because that is the line that goes invisible on dark grounds: `PRESENTER · TEAM` is `#9AA6BC` at 7.08:1 and is plainly legible in both slide-01.png and slide-06.png. The panels are only 1.51:1 against the canvas, so the gold hairline is what makes them read as panels — confirmed on slide-03.png and slide-04.png, where every panel edge is unambiguous.
- [x] Korean/CJK word-break integrity: PASS (not applicable) — this is an English deck; there is no Hangul or CJK and the four Pretendard faces the scaffolder installs were deleted. English wrapping was inspected in their place and the equivalent faults were found and fixed on the renders: the source caption wrapped to two lines on **all six sheets** and pushed content down under itself; two of three mechanism lines on slide-02 wrapped and dropped the gold total row onto the footline; the WHAT panel's serif requirement on slide-04 wrapped and burst its panel; and three panels ended on one-word runt lines ("most.", "awake.", "does."), all rewritten. In the current render **no line anywhere ends in an orphan word, no heading wraps, and no footline runs to two lines** — checked sheet by sheet at full size.
- [x] Review Litmus: PASS — Three to five seconds per sheet. slide-02.png reads as "three ordinary grants, none of which ever ended" from the ledger structure before a sentence is read. slide-04.png reads as WHO / WHAT / WHY / WHEN IT LAPSED — the record's four fields — from the gold labels alone, which is the point of the sheet. slide-06.png reads as three numbered decisions. Stripping decoration is not available: there is no decoration. The visual vocabulary is serif type, navy panels and gold hairlines, exactly what the spec's Avoid list demands. On removable lines: eleven were in fact removed during the render passes — the slide-03 note lost a clause, slide-05's note lost four words, slide-04's WHY panel lost thirteen — and every one of those sentences got better, which is the usual sign the line was removable.

## Findings
| Slide | Finding | Severity | Fix | Status |
|-------|---------|----------|-----|--------|
| all | The footline source caption wrapped to two lines on every sheet, growing the footer and pushing `main`'s content down onto it. `validate` passed all six | Major | Fixed: both footline items were flex-shrinkable, so the caption was squeezed below its natural width; `flex:none` on both, and the caption cut from 80 to 66 characters | resolved |
| slide-01 | **The gold hairline under the cover display rendered as nothing at all** — the rule was simply absent from the PNG | Major | Fixed: `main` overflowed, and the flex algorithm shrank the 0.5pt rule (a flex item with `flex-shrink:1`) to zero height. `flex:none` on `.rule-gold` and `.panel .hair` pins every hairline in the deck | resolved |
| slide-02 | The gold total row slid out of `main` and printed on top of the footline | Major | Fixed: two of three mechanism lines wrapped because the pre-write coefficient was 12% low; all three rewritten to ≤66 characters, row padding 11 → 6pt | resolved |
| slide-03 | The closing note wrapped to two lines and printed on top of the footline | Major | Fixed: note cut from 98 to 79 characters; panel mechanism copy cut so each panel takes four lines, not five | resolved |
| slide-04 | The WHAT panel's requirement wrapped, the WHY panel's reason ran to three lines, and both burst their gold-hairline panels onto the sheet below | Major | Fixed: serif requirements cut to ≤30 characters (the real 291pt measure is 34, not the 38 the bad probe implied), the WHY reason cut from 84 to 57 | resolved |
| slide-05 | The closing note wrapped to two lines and printed on top of the footline | Major | Fixed: note cut from 92 to 76 characters | resolved |
| slide-06 | The two columns were both vertically centred but are different heights, so their first lines sat ~26pt apart — the "columns whose first lines do not align" defect | Minor | Fixed: both columns top-aligned, and the left column given a closing recap line so it fills its share instead of leaving a dead navy band above the presenter line | resolved |
| slide-01 | The left prose and the right panel started at different y, because the panel's 12pt inner padding offsets its first line | Minor | Fixed: matching `padding-top:12pt` on the premise | resolved |
| slide-02 | The last row's 1px navy rule sat ~4pt above the gold total rule and read as one doubled line | Minor | Fixed by colouring the last row's border `transparent` rather than removing it, so every row keeps an identical box | resolved |
| slide-03, 05 | Three panels ended on a one-word runt line ("most.", "awake.", "does.") | Minor | Fixed: all three rewritten; panels on 05 now take three lines each and on 03 four lines each | resolved |
| slide-03, 05 | The gold chevrons in slide-05's gutters are small at 9 × 14px | Note | Accepted — the spec asks for "restrained chevron arrowheads" and a larger one would read as an icon, which the Avoid list forbids. Carried to design-debt.md #3 | tracked |
| all | `<header>`·`<footer>` 를 `<div>`로, `<p>` 밖 `<span>`을 `<p>`로 바꿨다. 파워포인트 text 엔진은 두 경우 모두 글자를 **경고 없이 버린다** | Major | 실제 PPTX를 열어 HTML과 텍스트를 대조해 발견. 렌더 픽셀 차이 0 | fixed |
## Template Fidelity Report

Status: not-applicable

## Slide Fingerprints

- slide-01.html: 548e554a0ad33f1235544fc976ef22775154e2873db269f934ec990cf111274e
- slide-02.html: ee19e577cba88829fb40960db51e5f5d1eb51745d590688d234acb035a06986d
- slide-03.html: f532644eec63e5a519e9bd0b7cdb8b5cd2bb782ce953660e2a9e0fefb0b47909
- slide-04.html: 45a22a868067477c2d8a2b062c9e665a754940674fa13ad8bce15899af186514
- slide-05.html: 8cacb56730df5196c4f03be94b20796c1103862b4b155b7be962a3aee2797873
- slide-06.html: b7e3771b82cbe4147dcbfefc59f5f48437d7ed465aab986a27e8e6b8848ec66b
